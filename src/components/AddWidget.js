import React from 'react';
import { Box } from '@mui/material';

export default function AddWidget() {
  return (
    <Box sx={{ padding: 2 }}>
      {/* Content related to adding widgets has been removed */}
    </Box>
  );
}
